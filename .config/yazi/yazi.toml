# Quant BSPWM - Yazi Configuration

[manager]
ratio          = [1, 4, 3]
sort_by        = "natural"
sort_sensitive = false
sort_reverse   = false
sort_dir_first = true
linemode       = "size"
show_hidden    = true
show_symlink   = true

[preview]
tab_size   = 4
max_width  = 900
max_height = 600
cache_dir  = ""
image_filter = "triangle"
image_quality = 75
sixel_fraction = 15
ueberzug_scale = 1
ueberzug_offset = [0, 0, 0, 0]

[opener]
edit = [
    { run = '${EDITOR:-nvim} "$@"', block = true, for = "unix" },
]
open = [
    { run = 'xdg-open "$@"', desc = "Open", for = "unix" },
]
reveal = [
    { run = 'xdg-open "$(dirname "$1")"', desc = "Reveal", for = "unix" },
]
extract = [
    { run = 'unar "$1"', desc = "Extract here", for = "unix" },
]
play = [
    { run = 'mpv "$@"', orphan = true, for = "unix" },
]
view_image = [
    { run = 'feh "$@"', orphan = true, for = "unix" },
    { run = 'sxiv "$@"', orphan = true, for = "unix" },
]

[open]
rules = [
    { name = "*/", use = [ "edit", "open", "reveal" ] },
    { mime = "text/*", use = [ "edit", "reveal" ] },
    { mime = "image/*", use = [ "view_image", "open", "reveal" ] },
    { mime = "video/*", use = [ "play", "open", "reveal" ] },
    { mime = "audio/*", use = [ "play", "open", "reveal" ] },
    { mime = "inode/x-empty", use = [ "edit", "reveal" ] },
    { mime = "application/json", use = [ "edit", "reveal" ] },
    { mime = "application/zip", use = [ "extract", "reveal" ] },
    { mime = "application/gzip", use = [ "extract", "reveal" ] },
    { mime = "application/x-tar", use = [ "extract", "reveal" ] },
    { mime = "application/x-bzip2", use = [ "extract", "reveal" ] },
    { mime = "application/x-7z-compressed", use = [ "extract", "reveal" ] },
    { mime = "application/x-rar", use = [ "extract", "reveal" ] },
    { mime = "application/pdf", use = [ "open", "reveal" ] },
    { mime = "*", use = [ "open", "reveal" ] },
]

[tasks]
micro_workers    = 10
macro_workers    = 25
bizarre_retry    = 5
image_alloc      = 536870912
image_bound      = [0, 0]
suppress_preload = false

[input]
cd_title  = "Change directory:"
cd_origin = "top-center"
cd_offset = [0, 2, 50, 3]

create_title  = "Create:"
create_origin = "top-center"
create_offset = [0, 2, 50, 3]

rename_title  = "Rename:"
rename_origin = "hovered"
rename_offset = [0, 1, 50, 3]

trash_title   = "Move {n} to trash? (y/N)"
trash_origin  = "top-center"
trash_offset  = [0, 2, 50, 3]

delete_title   = "Delete {n} permanently? (y/N)"
delete_origin  = "top-center"
delete_offset  = [0, 2, 50, 3]

find_title  = [ "Find next:", "Find previous:" ]
find_origin = "top-center"
find_offset = [0, 2, 50, 3]

search_title  = "Search via {n}:"
search_origin = "top-center"
search_offset = [0, 2, 50, 3]

shell_title  = [ "Shell:", "Shell (block):" ]
shell_origin = "top-center"
shell_offset = [0, 2, 50, 3]

overwrite_title  = "Overwrite? (y/N)"
overwrite_origin = "top-center"
overwrite_offset = [0, 2, 50, 3]

quit_title  = "{n} task(s) running, quit? (y/N)"
quit_origin = "top-center"
quit_offset = [0, 2, 50, 3]

[select]
open_title  = "Open with:"
open_origin = "hovered"
open_offset = [0, 1, 50, 7]

[log]
enabled = false
